---
description: Project each candidate option (screens, effort band, risks, constraints) side-by-side before /decide, plus the value-of-information list of decision-flipping Unknowns.
argument-hint: "[O-NNN ...]"
---

Invoke the `aisa-simulate` skill with the arguments provided.

Args: $ARGUMENTS
