---
description: Start a new aisa engagement (scaffold + capture the request).
argument-hint: "<slug> [pack]"
---

Invoke the `aisa-start` skill with the arguments provided.

Args: $ARGUMENTS
