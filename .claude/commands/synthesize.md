---
description: Produce the 5 topic packs in _synthesis/ from SU + lens-outputs + decisions (auto-runs after /decide; this command is for manual retries).
argument-hint: "[<topic>]"
---

Invoke the `aisa-synthesize` skill with the arguments provided.

Args: $ARGUMENTS
