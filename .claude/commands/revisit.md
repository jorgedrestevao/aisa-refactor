---
description: Compare the present with a frozen counterfactual when a decision tripwire fires; recommend keep / adapt / reopen Options (advisory — never changes the decision).
argument-hint: "<TW-n | O-NNN>"
---

Invoke the `aisa-revisit` skill with the arguments provided.

Args: $ARGUMENTS
