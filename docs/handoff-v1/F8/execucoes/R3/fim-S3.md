# FIM DE SEGMENTO F8 — R3 / S3

- **feito**:
  - `/decide`: obituário primeiro (premortem.md, 6 causas de morte, 5 requisitos + 4 tripwires candidatos), depois decisão registada — D-002, adopta O-002 (app orientada a registos/model-driven sobre a loja governada da plataforma imposta). Justificação, alternativas, assumptions aceites (A-001/A-002), riscos aceites (R-003, R-004, U-011, U-013), 4 tripwires (TW-1..TW-4, TW-4 vindo do premortem). Sponsor confirmation: pending.
  - `/synthesize`: 5/5 pacotes de tema produzidos (business-story, as-is, risks-and-assumptions, financial-story de imediato; architecture-story mais tarde, quando o blueprint deu autoridade de arquitectura para citar).
  - `/blueprint`: reconciliação prévia publicada (`_coverage/coverage_v04.json`, 83/83 unidades, `eligible: true`), `ux-blueprint_v01.yaml` produzido e estruturalmente válido (0 bloqueios, 14 avisos — todos o mesmo achado de ferramenta, ids `M-n` não reconhecidos pelo padrão do motor). 4 entidades, 5 perfis, 3 ecrãs. Mecanismo de auditoria desenhado (fecha U-006 por desenho). Revisão de cobertura da versão publicada (`_coverage/coverage_v09.json`, a mais recente e válida): `coverage: gaps`, `eligible: false`.
  - Perguntei directamente ao dono (3 rondas de PEDIDO F8) sobre U-002, U-004, U-012, U-011 — respondeu "não sei" às quatro; registadas em `council-log.md` sem fechar nenhuma (mesma forma que X-001 em R-02).
- **estado**: fase `decision`, ronda `D-01`. Publicado: `decisions.md#D-002` (D-01), `_synthesis/*.md` (5/5), `premortem.md`, `_blueprint/ux-blueprint_v01.yaml`, `_coverage/coverage_v04.json` (reconciliation) e `_coverage/coverage_v09.json` (blueprint, a válida — v05/v06/v07/v08 ficam na história como tentativas corrigidas). `story.md` com 2 episódios novos (obituário, blueprint).
- **perguntas abertas e bloqueios**:
  - **Bloqueio real, não contornável**: `open_architecture_choices[0]` do blueprint — autoridade e mecanismo de integração do catálogo de preços (tempo real via ligação ao ERP-X vs cópia própria) — é **estrutural** (`blueprint-contract.md` regra 5) e bloqueia a **aprovação** da versão (nunca a produção, que já aconteceu). Sem base A/B/C disponível: ninguém neste piloto sabe se o ERP-X tem interface programável (U-012) nem, por consequência, se compensa ler em tempo real ou copiar (U-004). Não há override — é uma escolha por natureza técnica, fecha só com facto.
  - Consequência directa: **`/blueprint` passo 15 (aprovação) não foi pedido** — a etapa (ii) do portão (cobertura da versão) recusa-o mecanicamente enquanto a escolha estrutural estiver aberta. Passos **15b (âmbito da entrega), 16 (autorização de contratos funcionais) e 17 (inventário de trabalho) ficam por isso também bloqueados** — dependem todos da aprovação anterior.
  - Perguntas em aberto materiais que sobram: U-002 (contexto de uso — "não sei"), U-011 (entitlement — "não sei", aceite como risco), U-013 (instalabilidade app móvel, condicional a U-002), R-004 (cópia do catálogo, contingente de U-004), X-001 (Conflicted, caminho de urgência — deslocado para antes da entrega final, não do blueprint, por autoridade de D-001).
  - Excluídas do âmbito deste blueprint com autoridade (D-002/D-001), não bloqueiam nada: U-001, U-007, U-008, U-009, U-010 (obrigações financeiras/de negócio, não de arquitectura de ecrãs) — projectadas em `_synthesis/`.
  - Anomalia sinalizada, não corrigida (fora do âmbito): R-001/R-002 continuam `resolved=false` na SU apesar de R-003/R-004 as citarem como sucedidas ("was") — possível defeito de escrita de uma ronda anterior (O-01), não deste segmento.
- **próximo passo que o aisa indica** (`dashboard.py --json` → `status.milestone`, literal): marco "aprovar blueprint v01"; comando `/answer U-004 "..." → /blueprint --refresh`. Ou seja: só uma resposta técnica real a U-012/U-004 (ou uma decisão explícita do dono de assumir esse risco de outra forma, o que o motor não aceita como fecho) destrava o resto da cadeia.
- **anomalias** (literal):
  - Ferramenta: `ID_RE` partilhado por `coverage.py`/`dashboard.py --blueprint-check` não reconhece o prefixo `M-` (invariantes do enquadramento) — gera 14 avisos `BP-BAD-ID` inofensivos em todo `su_refs` que cite `M-1`..`M-4`. Não bloqueia nada; acompanha o desenho pela vida fora do engagement.
  - Motor: `R-001`/`R-002` não transitam para `resolved=true` quando `R-003`/`R-004` as citam como `was` — já visto na RETOMA inicial, confirmado aqui outra vez em duas revisões de cobertura distintas.
  - Instrução de skill não coberta explicitamente: `aisa-synthesize` assume que os 5 pacotes se produzem todos logo a seguir a `/decide`, mas `architecture-story.md` depende de uma autoridade de arquitectura que só existe depois de `/blueprint` correr — produzi 4/5 nessa altura e o 5º mais tarde, quando ficou possível (`dashboard.py --authority-stamp` passou de exit 3 para exit 0).
  - Sem workbook `.xlsx` neste engagement (só `.md`) — `fields_draft.py` não tem nada para processar; todos os campos do blueprint ficam `state: Assumed`/`Confirmed` por design ou por SU, nunca por L1.
