# FIM DE SEGMENTO F8 — R3 / S1

- **feito**:
  - `/start f8-r3-fx02 pp` — engagement nascido (grafo, birth draft, `workflow.py check` ok), 4 ficheiros capturados (`_capture/`, capture-lite, run 1), enquadramento com 4 invariantes (M-1..M-4) + autoridade da plataforma (C-001).
  - `/round` (passagem R-01, as seis perspectivas) — análise integrada publicada, árbitro corrido, 3 rondas de revisão independente (subagente `lens-coverage-reviewer`) até as seis ficarem `treated`, registo de cobertura `_coverage/coverage_v01.json` (v01) publicado, passagem fechada (revista, não «por rever»).
  - `/answer` a 2 perguntas (U-005 → C-006; U-003 → C-007); as outras 9 ficaram registadas como perguntadas e sem resposta, sem fechar.
- **estado**: fase `discovery`, ronda `R-01` (fechada), sem passagem em curso. SU: 11 Confirmed (C-001..C-007, M-1..M-4) · 5 Assumed (A-001..A-005) · 8 Unknown em aberto (U-001, U-002, U-004, U-006, U-007, U-008, U-009, U-010) · 1 Conflicted em aberto (X-001, Critical) · 2 Risky (R-001, R-002). Cobertura `_coverage/coverage_v01.json` — válida à data em que fechou a passagem, mas `freshness` já `stale` para uma leitura nova (as duas respostas mudaram a SU depois do registo; normal, não bloqueia nada já feito, só diz a quem for reler para verificar de novo antes de usar como base).
- **perguntas abertas e bloqueios**: bloqueio material único — `X-001` (Critical): o caminho de urgência salta a aprovação da chefia, mas ninguém sabe dizer se também salta o limiar financeiro de 5 000 € (agora conhecido via C-006). Esta é a pergunta que o `next_action` do motor aponta como a resolver antes de `/frame`. As outras 7 perguntas abertas (U-001, U-002, U-004, U-006, U-007, U-008, U-009, U-010) não são Critical, mas alimentam esforço, custo e desenho.
- **próximo passo que o aisa indica**: resolver `X-001` com o sponsor antes de `/frame` (`next_action.command`: `/answer X-001 "..."`). Não corri `/frame` — fora do âmbito deste segmento.
- **anomalias** (literal):
  - Enganei-me a chamar `text_extract.py` sem a extensão do ficheiro original no nome de saída (produzi `pedido.extraction.json` em vez de `pedido.md.extraction.json`); o índice de evidência ficou `not captured` para as 4 fontes até eu detectar e corrigir (reextrair com o nome certo). Ficou no `_capture-log.md`, que é append-only — as duas tentativas são histórico real.
  - Escrevi as 4 linhas do enquadramento (invariantes do dono) com id `M-1..M-4` e depois tentei "corrigir" para `C-002..C-005`, por ler mal `library/kernel/states.md` ("Id prefixes: C-/A-/U-/X-/R-"). O guarda de append-only recusou a publicação (`SU_ROW_REMOVED`) — e bem: `dashboard.py` documenta explicitamente `M-1` como forma válida de id de linha (`ROW_ID_RE`, comentário na linha 253) para as linhas de enquadramento R-00. Abandonei o rascunho, sem dano.
  - O motor (`dashboard.py`) exige `impacto` na forma exacta `aspecto: frase` (com dois-pontos) e recusa/assinala aspectos "soltos" sem frase — não é óbvio pela prosa da skill (`lens-checklists.md` mostra o formato num sítio só). Tive de corrigir 8 linhas `Unknown`/`Conflicted` depois de publicadas (não é transição — é o árbitro a completar campo, como a skill prevê no passo 5c-2).
  - O revisor independente (`lens-coverage-reviewer`) apanhou 3 lacunas reais nas minhas próprias disposições/justificações em duas rondas de revisão (critério de sucesso descartado por engano; arredondamento mal classificado como `assessed`; envelope de custo as-is nunca escrito). As três eram achados correctos, não falsos positivos — corrigi todas antes de fechar. Fica registado porque mostra o revisor a fazer o trabalho para que existe, não como falha do processo.
  - `SubagentHandback` recusou uma segunda chamada consecutiva ("já entregue... usa SendMessage"), obrigando a mudar de ferramenta a meio da tarefa; pouco depois o sistema forçou o regresso a `SubagentHandback` («handback-send-enforce»). Reporto literal — não é nada que eu tenha causado, é comportamento do harness.
  - `coverage.py finalize` exige `version` e `generated_at` preenchidos à mão no rascunho (`lens-draft` não os preenche), e qualquer edição à SU depois de gerar o esqueleto obriga a regenerá-lo (`STALE` na base) — tive de repetir `lens-draft` uma vez a meio das correcções. Mecânico, não é bug, mas custa passagens extra.
